{
  "name": "muj-side-hustle",
  "version": "1.0.0",
  "scripts": {
    "dev": "tinacms dev -c \"echo 'Running dev'\"",
    "build": "tinacms build && echo 'Build done'",
    "start": "echo 'No start script needed'"
  },
  "dependencies": {
    "@tinacms/cli": "latest",
    "tinacms": "latest"
  }
}